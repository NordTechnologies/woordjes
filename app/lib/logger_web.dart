// Web stubs — logs are only kept in the installed app.
Future<void> initLog() async {}
void logEvent(String msg) {}
Future<void> shareLogs() async {}
String recentLogs() => 'Logs are only kept in the installed app.';
